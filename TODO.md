- [ ] Add timeouts and yields to Firebase calls in readAndControl() to prevent blocking
- [ ] Add timeouts and yields to Firebase calls in pushHistory() to prevent blocking
- [ ] Add timeouts and yields to Firebase calls in updateLiveData() to prevent blocking
- [ ] Add timeouts and yields to Firebase calls in sendAlertToAll() to prevent blocking
- [ ] Test the changes for ESP8266 resets
